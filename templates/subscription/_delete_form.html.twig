<form method="post" action="{{ path('subs_delete', {'id': subs.getId()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subs.getId()) }}">
    <button class="btn">Delete</button>
</form>
