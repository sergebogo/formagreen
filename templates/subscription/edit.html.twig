{% extends 'base.html.twig' %}

{% block title %}Edit subscription{% endblock %}

{% block body %}
    <a title="Retour vers la liste des souscription" class="btn btn-default btn-sm"
       href="{{ path('subscription_index') }}"><i
                class="material-icons">arrow_back</i> Retour</a>

    {{ include('subscription/_form.html.twig', {'button_label': 'Update'}) }}

    <p class="d-inline font-weight-bold text-bold">Supprimer </p>
    {{ include('subscription/_delete_form.html.twig') }}
{% endblock %}
