<form method="post" action="{{ path('member_delete', {'id': member.id}) }}" onsubmit="return confirm('Vous êtes sûr de supprimer cet element?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member.id) }}">
    <button class="btn">Supprimer</button>
</form>
