<form class="d-inline" method="post" action="{{ path('partnership_delete', {'id': partner.getId()}) }}"
      onsubmit="return confirm('Vous êtes sûr de supprimer cet element?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner.getId()) }}">
    <button class="btn btn-sm btn-danger"><i class="material-icons">delete</i></button>
</form>
